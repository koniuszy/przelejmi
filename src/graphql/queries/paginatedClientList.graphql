query paginatedClientList($orderBy: [ClientOrderByInput!], $where: ClientWhereInput) {
  paginatedClientList: paginatedClients(orderBy: $orderBy, where: $where) {
    totalCount
    list {
      id
      name
      address
      postCode
      city
      country
      nip
    }
  }
}
